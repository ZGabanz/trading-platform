# Frontend Environment Variables
NODE_ENV=production
# NEXT_PUBLIC_API_URL is intentionally NOT set to prevent direct backend calls from browser
# The frontend uses Next.js API routes which handle backend communication and provide mock data

# Optional: Analytics
NEXT_TELEMETRY_DISABLED=1 